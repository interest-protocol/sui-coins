import { Box, Button } from '@interest-protocol/ui-kit';
import { FC } from 'react';

const SendButton: FC = () => (
  <Box display="flex" justifyContent="center">
    <Button variant="filled">Create Link</Button>
  </Box>
);

export default SendButton;
